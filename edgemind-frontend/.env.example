# URL of the running EdgeMind backend (FastAPI). No auth, no keys needed here —
# the Gemini API key lives server-side only and is never touched by the frontend.
VITE_API_BASE_URL=http://localhost:8000
